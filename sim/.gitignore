# Build output, and the emulated device's own storage: both are generated by running the emulator.
build/
state/
sdcard/
build-tests/

# Bus logs are recordings, often large.  Commit one deliberately with `git add -f` if a scenario
# is worth keeping in the repository.
*.log
