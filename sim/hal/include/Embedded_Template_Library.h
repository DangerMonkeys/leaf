// ETL's Arduino-distribution marker header.
//
// The PlatformIO package ships only include/etl, so this header -- which src/vario/ui/display/
// menu_page.h includes -- has no file behind it in a host build.  In the Arduino distribution it
// exists only to make the IDE link the library; nothing depends on its contents.
#pragma once
